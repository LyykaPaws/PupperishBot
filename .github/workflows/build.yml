name: Runs

on:
  push:
    branches: [ "stable" ]
  workflow_dispatch:
    branches: [ "stable" ]

jobs:
  build:
    name: Run
    runs-on: [self-hosted, Linux, X64]
    steps:
    - name: Checkout repository
      uses: actions/checkout
    - name: Restore dependencies
      run: npm i tmi.js